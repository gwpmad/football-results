<!DOCTYPE html>
<html>
  <head>
      <meta charset="utf-8">
      <link rel="stylesheet" type="css" href="./main.css">
      <!-- I think the below works because the views in the main views folder
      need it (or they look for it in (e.g.) points/bower_components and the
      main.handlebars layout (which doesn't put its name in before bower_components
      for some reason) just tries to cd one folder up and can't - it thinks it's in the
      root. -->
      <link rel="stylesheet" href="../bower_components/bootstrap/dist/css/bootstrap.min.css">
      <title>Football Stats!</title>
  </head>
  <body>
      <div id='top-bar'>
        <h1>Football Stats!</h1>
      </div>
      {{{body}}}
  </body>
</html>
